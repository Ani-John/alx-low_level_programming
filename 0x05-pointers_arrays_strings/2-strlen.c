#include "main.h"
#include <string.h>
/**
 * _strlen - a function that returns the length of a string.
 *
 * @s: the string
 *
 * Return: the measured string length
 */

int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
