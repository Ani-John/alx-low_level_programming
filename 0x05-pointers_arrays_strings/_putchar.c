#include <stdio.h>
#include "main.h"
/**
 * _putchar - a function that prints 'putchar'
 * @i: the value to be printed
 * Return: 0
 */
char _putchar(char i)
{
        putchar(i);
        return (0);
}
